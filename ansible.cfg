[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False
stdout_callback = default
interpreter_python = auto_silent
timeout = 20
private_key_file = SSH-Key-Location

[privilege_escalation]
become = True
become_method = sudo
become_ask_pass = False
